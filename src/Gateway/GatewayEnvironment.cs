namespace Sufficit.Gateway
{
    /// <summary>
    /// Identifies the remote environment selected for a provider call.
    /// </summary>
    public enum GatewayEnvironment : byte
    {
        Sandbox = 0,
        Production = 1
    }
}
