namespace Sufficit.Finance
{
    /// <summary>
    /// Identifies the external provider environment used by a tenant configuration.
    /// </summary>
    public enum BankSlipProviderEnvironment : byte
    {
        Sandbox = 1,
        Production = 2
    }
}
